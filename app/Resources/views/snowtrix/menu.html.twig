<nav id="menu">
    <div class="inner">
        <ul>
            <li><a href="{{ path('homepage') }}">Accueil</a></li>

            {% if is_granted('ROLE_USER') %}
                <li><a href="{{ path('fx_user_logout') }}">Déconnexion</a></li>
            {% else %}
                <li><a href="{{ path('fx_user_login') }}">Connexion</a></li>
                <li><a href="{{ path('fx_user_register') }}">Inscription</a></li>
            {% endif %}
        </ul>
        <a href="#" class="close">Close</a>
    </div>
</nav>